import { Heading, Stack, Box } from "@chakra-ui/react";

export default function Contact() {
  return (
    <Stack spacing={2} maxW={500} lineHeight="taller">
      <Heading>Contact</Heading>
      <Box>The best way to contact us is...</Box>
    </Stack>
  );
}
